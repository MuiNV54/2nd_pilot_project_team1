<%= form_for [current_user, @album] do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :description %>
  <%= f.cktext_area :description %>
  <%= f.submit "Create" %>
<% end %>