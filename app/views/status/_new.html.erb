<%= form_for [current_user, @status] do |f| %>
  <div class="stt_area">
    <%= f.cktext_area :content,:ckeditor => {:toolbar => "MyToolbar"}, placeholder:"What are you feeling?" %>
  </div>
	<div><%= f.submit "Post" %></div>
<% end %>